spring:
  application:
    name: coursemanagement

  datasource:
    url: jdbc:postgresql://localhost:5432/course_management
    username: postgres
    password: phongminer12

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

    database-platform: org.hibernate.dialect.PostgreSQLDialect